import type { EventBus } from "../../shared/contracts/event-bus.interface";

export function registerTaskEventListeners(
  eventBus: EventBus,
): void {
  void eventBus;
}

// Event listeners will be registered here
// when external domain events are defined.